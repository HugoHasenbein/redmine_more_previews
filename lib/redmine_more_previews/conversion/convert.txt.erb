Hello World!

You were trying to preview the following object: <%= @object["object"].class.name %>

The following parameters were available for conversion of the object:
<% instance_variables.each do |ivar| %>
<%= ivar.to_s.ljust(20, " ") %><%= instance_variable_get(ivar).to_s.html_safe %>
<% end %>
